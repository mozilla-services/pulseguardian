# Web app
flask_host = 'localhost'
flask_port = 5000
flask_secret_key = None # Should be generated randomly
flask_debug_mode = True

# Mail
email_account = 'automation@mozilla.com'
email_password = None # Email's password goes here
email_from = 'Mozilla A-Team <auto-tools@mozilla.com>'

# Database
sqlalchemy_engine_url = 'mysql://root@localhost/pulseguardian'

# RabbitMQ
rabbit_host = 'localhost'
rabbit_management_port = 15672
rabbit_vhost = '/'
rabbit_user = 'guest'
rabbit_password = 'guest'

# PulseGuardian
warn_queue_size = 2000
del_queue_size = 8000
polling_interval = 5

# Logging
GUARDIAN_LOG_PATH = 'log_guardian.log'
MAX_LOG_SIZE = 20480

# Overriding configuration with os environment vars (e.g Stackato)
import os
import urlparse

if 'DATABASE_URL' in os.environ:
    sqlalchemy_engine_url = os.environ['DATABASE_URL']
if 'RABBITMQ_URL' in os.environ:
    url = urlparse.urlparse(os.environ['RABBITMQ_URL'])
    rabbit_host = url.hostname
    rabbit_user = url.username
    rabbit_password = url.password